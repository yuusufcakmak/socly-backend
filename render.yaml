services:
  - type: web
    name: socly-backend
    env: python
    buildCommand: ""
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: FLASK_ENV
        value: production
